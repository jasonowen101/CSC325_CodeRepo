package LoopingExamples;

public class Lance_Roberts_LoopExample {
    public static int whileLoopWithBreak(String[] args) {
        while (index < 3) {

            int counter;
            int index;

            for (int index : ArrayName) {
                counter++;
                index++;
            }

            System.out.println("Counter = ", counter);
            System.out.println("Index = ", index);
        }
    }

public static void forLoopWithBreak() {
    for (int i = 1; i <= 1000; i++) {
        int counter1;

        if (counter1 == 100){
            System.out.println("We found 100 iterations.");
            break;
        }
    }

    public static void main(String [] args) {
        int [] arr = {1,2,3,4};

        int countWhileLoop = whileLoopWithBreak(arr);
        System.out.println()
    }

    }

    }

    int[] arr = { 1, 2, 3, 4 };

    // JTO Creating a whileLoop method to break at index 3 /// done

    // JTO Instantiate counter and index variables /// done

    // JTO Loop through a passed in array /// done maybe

    // JTO Perform any desired operation inside the loop /// done
    // JTO For this example, we'll just increment the count & index /// done

    // JTO once loop is broken then return the iteration count /// done

    // JTO Create a method to do a for loop and break at 100 /// done

    // JTO Create the for loop to loop up to 1000 times /// done

    // JTO Break the loop when counter reaches 100 /// done

    // JTO Output the user we found 100 iterations and break the loop /// done

    // JTO Main to call the loop methods

    // JTO Instantiate a basic array of integers

    // JTO Call the method with while loop

    // JTO Call the method with for loop
}
