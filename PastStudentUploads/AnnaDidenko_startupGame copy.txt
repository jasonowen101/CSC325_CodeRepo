
DECLARE an int variable to hold the number of guesses, named numOfGuesses

    COMPUTE a random number between 0 and 4 that will be the starting location cell position

    WHILE the Startup is still alive GET user input from the command line






import java.util.Scanner;

public static void main(String[] args)

private static int Genran() {
        Random random = new Random();
        return random.nextInt(4); 
}

public class BattleshipGame {

    public static void main(String[] args) {
        int numOfguesses = 0;

        // from Startup class
        Startup startup = new Startup(Genran());

        Scanner scanner = new Scanner(System.in);

 ///// Doesn't have to be Get??///


        while (startup.isAlive()) { // clarify in Startup 
            System.out.print("Enter your guess (0-4): ");
            int userGuess = scanner.nextInt();

            // checkHit in the Startup 
            String result = startup.checkHit(userGuess);
            System.out.println("Result: " + result);

            ;
        }

        System.out.println("You sunk the ship using " + numOfguesses + " guesses.");
    }
   
}
